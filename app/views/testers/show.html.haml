%h2= @test.title
= simple_form_for @result do |form|
  %p.question= @test.question
  = render @test
  = form.input :comments, as: :text, label: "Other comments"
  .form-actions
    = form.button :submit, "Submit your answer!"